/* ============================================================
   Настройки за изпращане на имейли (EmailJS)

   Тук се въвеждат ключовете от акаунта в https://emailjs.com.
   publicKey е предназначен за браузъра и не е таен, но всеки,
   който отвори сайта, го вижда — затова в EmailJS ограничете
   позволените домейни (Account → Security → Allowed origins).
   ============================================================ */

const EMAIL_CONFIG = {
  enabled: true,

  publicKey: 'B2YPA4yp6vwMWlh7w',
  serviceId: 'service_8it0oal',

  // шаблон за клиента (потвърждение) и за салона (известие)
  clientTemplateId: 'template_m6a6lwo',
  businessTemplateId: 'template_o2z5dfi',

  // на този адрес идват известията за нови резервации
  salonEmail: 'ilievdilyan@gmail.com'
};

/* ============================================================
   Връзка с Google Календар през Google Apps Script

   Web App-ът приема:
     GET  ?date=YYYY-MM-DD  →  { status: "success", busySlots: [...] }
     POST { ...резервация }  →  създава събитие в календара

   busySlots се приема в няколко формата:
     ["09:00", "13:30"]                        начални часове
     [{ start: "09:00", end: "10:30" }]         интервали
     [{ start: "09:00", duration: 90 }]         начало + минути
   Записите могат да носят и "staff" с id на специалист — тогава
   заемат само неговия график, вместо целия салон.
   ============================================================ */

const CALENDAR_CONFIG = {
  enabled: true,

  webAppUrl: 'https://script.google.com/macros/s/AKfycbzwqelKebMwkjPqzMPIKiOGo3IdGJ-H9XClkHG6ZPzwCiPzdvtV4eIcAWi6Et3eTBhkjA/exec',

  // записва ли новите резервации обратно в календара
  sendBookings: true,

  // колко трае зает час, ако календарът върне само начало
  defaultBusyMinutes: 60,

  // един общ календар за студиото: зает час блокира всички специалисти
  sharedCalendar: true,

  /* Календарът е единственият източник на истина за заетостта.
     Часовете се теглят наново при всяко отваряне на ден — нищо не се
     кешира между зарежданията. Резервациите, запазени в браузъра, служат
     само за списъка „Моите часове“ и НЕ блокират часове: щом изтриете
     събитие в календара, часът веднага се освобождава за всички.
     false → браузърът пак пази заетото локално (работи и без календар). */
  authoritative: true,

  // след колко секунди се отказваме от заявката
  timeoutSeconds: 5
};
